# AML — instructions for AI coding sessions

AML is a self-hosted desktop writing environment (macOS + Windows) built on plain markdown files kept in sync with a home NAS by a Syncthing sidecar bundled in the app (Local-only and SMB Network modes also exist). It combines a linked-note knowledge base, a polished WYSIWYG editor that reads and writes canonical markdown, and professional compile/typesetting.

## Read first, every session
1. `docs/04-AI-DEVELOPMENT-PLAYBOOK.md` — how work is done here (specs, DoD, session protocol)
2. `docs/02-ARCHITECTURE-DECISIONS.md` — accepted ADRs are binding
3. `docs/03-GLOSSARY-AND-NAMING.md` — use these words and no others
4. `docs/ARCHITECTURE.md` — current module map (created in Stage 0)
5. The spec for the work package you are doing: `docs/specs/WP-x.y-*.md`

## Hard rules
- Never write to user `.md` files except through the atomic-write command. Saves emit canonical AML markdown; syntax the parser does not model is preserved verbatim as a Raw node — never dropped.
- Nothing derived or per-device (SQLite index, window state, layouts) lives inside the Folio; everything under `.aml/` is synced and must be merge-friendly (one item per line).
- The Syncthing sidecar is managed only through the `sidecar::syncthing` module; never shell out to it elsewhere.
- No file-system access from the webview; all I/O is Rust commands exposed via generated bindings.
- Do not add dependencies without a justification line in the spec.
- Vocabulary is fixed (ADR-011): root folder = **Folio**, virtual group = **Bounding**, folder-with-manifest = **Project**. Never "vault", "collection", "study", "library", "workspace" for these.
- Every WP ends with `pnpm check` green and the docs listed in the spec updated.
- Ask before changing an ADR; propose a superseding ADR instead of editing code around it.

## Status
Run `pnpm sidecar:fetch` once after cloning (downloads the Syncthing binary; not committed).
Stage 0 and Stage 1 done (WP-1.1–1.7 + 1.1b; NAS pairing confirmed 2026-09-10). **Stage 2 complete** (WP-2.1–2.9: index, links, backlinks, tags, search, outline, templates + Daily, Boundings + Overview, palette + rebindable shortcuts; WP-2.10 Obsidian migration skipped by decision), 2026-09-11. Gate 2 manual QA (`docs/qa/stage-2.md`) is Bryce's to sign. Stage 3 in progress: WP-3.1 (writing modes), WP-3.2 (appearance), **WP-3.0 (visual refresh, ADR-013 — the Apple-light foundation)**, WP-3.10 (Home in the breadcrumb, a three-tab Browser, the Settings screen and the Daily notes folder) and WP-3.3 (Note Types) done 2026-09-11, WP-3.4 (Goals) WP-3.7 (Statistics) and WP-3.9 (callouts as real nodes — no longer Raw) done 2026-09-12. **Stage 5 started out of order, by decision**: WP-5.1 (Project manifest), 5.2 (Binder), 5.3 (Corkboard) and 5.8 (Project dashboard) done 2026-09-12, leaving 3.6 (Paste intelligence), 3.8 (Syntax reveal), 5.4 (Outliner), 5.5 (Stitch), 5.6 (Research) and 5.7 (Layouts). Quality Gates 3 and 5 cannot be signed yet: both wait on Bryce's QA. A Project is a folder with a `project.aml.yaml`: the folder is the structure and the manifest is only the order, what a compile leaves out, and the book's goal — the Binder is reconciled against the folder on every read, and a document's synopsis/label/status are its own front matter. A note's target (`target_words`, `deadline`) and its type are its own front matter; what you wrote today is per-device and never enters the Folio (ADR-004). A note's type is its own front-matter `type:` and nothing else; `.aml/types.yaml` holds only the colours and icons you chose, and a type's fields are its template's front-matter keys. `⌘,` is Settings (Folio preferences in `.aml/config.yaml`, alongside appearance); Appearance is `⌥⌘,`. The look is decided in `tokens.css` and nowhere else: a unit test fails any stylesheet that names a colour, a radius or a font size of its own, and interface icons come from `src/app/icons.tsx`, never typed as characters. Run `pnpm fonts:fetch` once after cloning too (bundled faces; not committed). **WP-7.3 (link graph) done 2026-09-12**, also out of order: ⌘⇧G draws the Folio clustered by Bounding, the Context panel carries the open note's corner, and the force layout in `features/graph/layout.ts` is pure and tested. **WP-7.1 (diagrams) done 2026-09-13**, with **WP-7.2 folded into it and Mermaid dropped by decision** (Q15): right-click in a note → Insert diagram gives a structured editor — people, mind and world shapes, nine kinds of link including the genogram ties, and groupings you can drag with their contents — and what lands in the note is an ordinary `.svg` in `assets/` that carries its own model in a `<metadata>` block, so it renders anywhere and re-opens here. The editor and the exported file are the same components (`DiagramBody.tsx` through `renderToStaticMarkup`), so the file is by construction what was on screen; editing overwrites that same file through `asset_write_text`. Mermaid fences still round-trip as ordinary code fences, so ADR-001 stands unamended. **Stage 4 started 2026-09-14 with WP-4.2 (Conflicts) and WP-4.3 (sync health), by decision ahead of 4.1.** Syncthing's `*.sync-conflict-*` copies are hidden from the Browser, the count and the index (`is_ignored_name`) but still reported by the watcher (`watch::passes`), and `conflicts.rs` is the only code that may write into `.aml/` — it refuses any path that is not a conflict copy. A resolution writes the whole text with `write_atomic` and sends everything given up to the Trash, never deletes, and is refused if the original's mtime moved. The comparison (`features/conflicts/merge.ts`) is two-way; it does not use a Snapshot as its base yet. **WP-4.1 (Snapshots) with WP-3.5's History screen done 2026-09-14**: `snapshots.rs` keeps `.aml/snapshots/<note>/<UTC stamp>[-label].md`, written once and never edited. There is no timer — before any write to a note (`Folio::write_note`, link rewrites), the text on disk is kept if this run has not done so for that note in 30 minutes and it is not already the newest Snapshot; a labelled one is kept before a restore and before a conflict resolution; a failed Snapshot never blocks a save. Restore writes the Snapshot's exact bytes. Retention runs at Folio open (7 days all, daily to 90, labelled for ever, always the newest — the last a clarification of ADR-006), configurable as `snapshots.*` in `.aml/config.yaml`. History is deliberately quiet: *Saved* in the status bar, the editor's right-click menu and the palette open it; there is no shortcut. The sync banner is only for a sync stuck while connected, never for being offline. **Stage 8 started out of order, by decision**: WP-8.1 (release builds) and WP-8.2 (auto-update) done 2026-09-12 — AML updates itself from a published GitHub release, releasing is `node scripts/version.mjs <v>` then a `v<version>` tag, and `docs/RELEASING.md` is the checklist. The one version lives in `package.json`, `Cargo.toml` and `tauri.conf.json` and `pnpm check` fails if they disagree; update bundles are signed with AML's own minisign key (not an OS certificate — the private half is in `~/.tauri/aml-updater.key` and the repository secrets, and losing it ends updating for every installed copy). **Quality Gate 8's second line is signed**: 0.2.0 → 0.3.0 updated itself on both machines on 2026-09-13, Windows with no administrator prompt (`docs/qa/results/2026-09-13-stage-8.md`). Gate 1 manual QA (`docs/qa/stage-1.md`) is Bryce's pending task; the Windows install is done. NAS setup steps are in `docs/06-NAS-SETUP.md`. `docs/CHANGELOG.md` is the per-WP log.
