# AML — instructions for AI coding sessions

AML is a self-hosted desktop writing environment (macOS + Windows) built on plain markdown files kept in sync with a home NAS by a Syncthing sidecar bundled in the app (Local-only and SMB Network modes also exist). It combines a linked-note knowledge base, a polished WYSIWYG editor that reads and writes canonical markdown, and professional compile/typesetting.

## Read first, every session
1. `docs/04-AI-DEVELOPMENT-PLAYBOOK.md` — how work is done here (specs, DoD, session protocol)
2. `docs/02-ARCHITECTURE-DECISIONS.md` — accepted ADRs are binding
3. `docs/03-GLOSSARY-AND-NAMING.md` — use these words and no others
4. `docs/ARCHITECTURE.md` — current module map (created in Stage 0)
5. The spec for the work package you are doing: `docs/specs/WP-x.y-*.md`

## Hard rules
- Never write to user `.md` files except through the atomic-write command. Saves emit canonical AML markdown; syntax the parser does not model is preserved verbatim as a Raw node — never dropped.
- Nothing derived or per-device (SQLite index, window state, layouts) lives inside the Folio; everything under `.aml/` is synced and must be merge-friendly (one item per line).
- The Syncthing sidecar is managed only through the `sidecar::syncthing` module; never shell out to it elsewhere.
- No file-system access from the webview; all I/O is Rust commands exposed via generated bindings.
- Do not add dependencies without a justification line in the spec.
- Vocabulary is fixed (ADR-011): root folder = **Folio**, virtual group = **Bounding**, folder-with-manifest = **Project**. Never "vault", "collection", "study", "library", "workspace" for these.
- Every WP ends with `pnpm check` green and the docs listed in the spec updated.
- Ask before changing an ADR; propose a superseding ADR instead of editing code around it.

## Status
Run `pnpm sidecar:fetch` once after cloning (downloads the Syncthing binary; not committed).
Stage 0 and Stage 1 done (WP-1.1–1.7 + 1.1b; NAS pairing confirmed 2026-09-10). **Stage 2 complete** (WP-2.1–2.9: index, links, backlinks, tags, search, outline, templates + Daily, Boundings + Overview, palette + rebindable shortcuts; WP-2.10 Obsidian migration skipped by decision), 2026-09-11. Gate 2 manual QA (`docs/qa/stage-2.md`) is Bryce's to sign. Stage 3 in progress: WP-3.1 (writing modes), WP-3.2 (appearance), **WP-3.0 (visual refresh, ADR-013 — the Apple-light foundation)**, WP-3.10 (Home in the breadcrumb, a three-tab Browser, the Settings screen and the Daily notes folder) and WP-3.3 (Note Types) done 2026-09-11, WP-3.4 (Goals) WP-3.7 (Statistics) and WP-3.9 (callouts as real nodes — no longer Raw) done 2026-09-12. **Stage 5 started out of order, by decision**: WP-5.1 (Project manifest), 5.2 (Binder), 5.3 (Corkboard) and 5.8 (Project dashboard) done 2026-09-12, leaving 3.5 (Snapshots UI), 3.6 (Paste intelligence), 3.8 (Syntax reveal), 5.4 (Outliner), 5.5 (Stitch — do WP-4.1's snapshot engine first), 5.6 (Research) and 5.7 (Layouts). Quality Gates 3 and 5 cannot be signed yet: Gate 3 waits on Bryce's QA, and Gate 5's manifest-merge line needs the Conflicts panel (WP-4.2), which does not exist. A Project is a folder with a `project.aml.yaml`: the folder is the structure and the manifest is only the order, what a compile leaves out, and the book's goal — the Binder is reconciled against the folder on every read, and a document's synopsis/label/status are its own front matter. A note's target (`target_words`, `deadline`) and its type are its own front matter; what you wrote today is per-device and never enters the Folio (ADR-004). A note's type is its own front-matter `type:` and nothing else; `.aml/types.yaml` holds only the colours and icons you chose, and a type's fields are its template's front-matter keys. `⌘,` is Settings (Folio preferences in `.aml/config.yaml`, alongside appearance); Appearance is `⌥⌘,`. The look is decided in `tokens.css` and nowhere else: a unit test fails any stylesheet that names a colour, a radius or a font size of its own, and interface icons come from `src/app/icons.tsx`, never typed as characters. Run `pnpm fonts:fetch` once after cloning too (bundled faces; not committed). **WP-7.3 (link graph) done 2026-09-12**, also out of order: ⌘⇧G draws the Folio clustered by Bounding, the Context panel carries the open note's corner, and the force layout in `features/graph/layout.ts` is pure and tested. **Stage 8 started out of order, by decision**: WP-8.1 (release builds) and WP-8.2 (auto-update) done 2026-09-12 — AML updates itself from a published GitHub release, releasing is `node scripts/version.mjs <v>` then a `v<version>` tag, and `docs/RELEASING.md` is the checklist. The one version lives in `package.json`, `Cargo.toml` and `tauri.conf.json` and `pnpm check` fails if they disagree; update bundles are signed with AML's own minisign key (not an OS certificate — the private half is in `~/.tauri/aml-updater.key` and the repository secrets, and losing it ends updating for every installed copy). Gate 1 manual QA (`docs/qa/stage-1.md`) is Bryce's pending task; the Windows install is done. NAS setup steps are in `docs/06-NAS-SETUP.md`. `docs/CHANGELOG.md` is the per-WP log.
